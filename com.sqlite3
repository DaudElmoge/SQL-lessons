--Relation DBs
--One to Many,Many to Many,One to One

--Parent and Children -> one to many
--Teachers and Students -> many to many (join tables)
--President & deputy , conductor and driver -> one to one (can share the primary key,the foreign's table pk has to be unique)

--customers,products,orders,order_items

CREATE TABLE IF NOT EXISTS customers(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    phone TEXT NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS orders(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    order_id TEXT,
    total INTEGER NOT NULL,
    created_at DATE,
    status TEXT NOT NULL,
    customer_id INTEGER REFERENCES customers(id)

);

INSERT INTO customers (name,phone) VALUES ("Jane",0765665)

SELECT *FROM customers;

INSERT INTO orders (order_id,total,status,customer_id) VALUES ("37HHD",1000,"Pending",1)
